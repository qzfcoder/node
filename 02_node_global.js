// 浏览器端
// bom window对象模型 api (loacltion, history)
// dom document对象模型  (对dom对象的操作)
// es规范

// node
// 没有dom
// 没有bom
// 有几乎全部的es规范
// 没有了window，而是一个global对象

// 在node中this不能指向global
console.log(this); // {}
console.log(global);
